package stackmc

/*
// Sample mat implements the RowMatrix interface
type sampleMat struct {
	samples [][]float64
}

func (s sampleMat) Dims() (r, c int) {
	r = len(s.samples)
	c = len(s.samples[0])
	return
}

func (s sampleMat) At(i, j int) float64 {
	return s.samples[i][j]
}

func (s sampleMat) Row()
*/
